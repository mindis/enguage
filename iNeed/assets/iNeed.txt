####################################################
### I need X
###
### The latest version of this file can be found at:
### http://www.yagadi.com/assets/iNeed.txt (c) yagadi.com, 2013-4.
### 
###On "X needs PHRASE-Y", perform "variable set subject X". # subject is use in populating list
####################################################

# Six main third-person utterances
     On "X needs PHRASE-Y", perform "list exists X needs Y".
Then on "X needs PHRASE-Y", reply "I know".
Then on "X needs PHRASE-Y", if not, perform "list add X needs Y".
Then on "X needs PHRASE-Y", reply "ok, X needs Y".

     On "X does not need PHRASE-Y", perform "variable set subject X".
Then on "X does not need PHRASE-Y", perform "list remove X needs Y".
Then on "X does not need PHRASE-Y", if not, reply "No, I do not know what you mean by Y". # this -> Y? Y="the y" needs to be Y="any y"
Then on "X does not need PHRASE-Y", reply "ok, X does not need Y".

     On "what does X need", perform "variable set subject X".
Then on "what does X need", perform "item set format 'QUANTITY,UNIT of,,from FROM'"
Then on "what does X need", perform "list get X needs".
Then on "what does X need", if not, reply "X does not need anything".
Then on "what does X need", reply "X needs ...".

     On "does X need PHRASE-Y", perform "list exists X needs Y".
Then on "does X need PHRASE-Y", if not, reply "no, X does not need Y".
Then on "does X need PHRASE-Y", reply "yes, X needs Y".

On "X has PHRASE-Y.", X does not need Y.

     On "X has everything.", perform "list delete X needs".
Then on "X has everything.", reply "ok, X does not need anything".

####################################
####################################
     On "X needs NUMERIC-QUANTITY PHRASE-Y", perform "variable set subject X". # subject is used in populating list
Then on "X needs NUMERIC-QUANTITY PHRASE-Y", perform "item set format 'QUANTITY,UNIT of,,from FROM'"
Then on "X needs NUMERIC-QUANTITY PHRASE-Y", perform "list exists X needs Y QUANTITY".  # check for equality with elaborates?
Then on "X needs NUMERIC-QUANTITY PHRASE-Y", reply "I know".
Then on "X needs NUMERIC-QUANTITY PHRASE-Y", if not, perform "list add X needs Y QUANTITY".
Then on "X needs NUMERIC-QUANTITY PHRASE-Y", reply "ok, X needs a total of ... ".
 
     On "X needs NUMERIC-QUANTITY UNIT of PHRASE-Y", perform "variable set subject X". # subject is used in populating list
Then on "X needs NUMERIC-QUANTITY UNIT of PHRASE-Y", perform "item set format 'QUANTITY,UNIT of,,from FROM'"
Then on "X needs NUMERIC-QUANTITY UNIT of PHRASE-Y", perform "list exists X needs Y QUANTITY UNITS". # simple list check?
Then on "X needs NUMERIC-QUANTITY UNIT of PHRASE-Y", reply "I know".
Then on "X needs NUMERIC-QUANTITY UNIT of PHRASE-Y", if not, perform "list add X needs Y QUANTITY UNITS".
Then on "X needs NUMERIC-QUANTITY UNIT of PHRASE-Y", reply "ok, X needs ...".

     On "how many PHRASE-Y does X need", perform "variable set subject X". # subject is used in populating list
Then on "how many PHRASE-Y does X need", perform "list quantity X needs Y".
Then on "how many PHRASE-Y does X need", reply "..., X needs ... Y".

On "how many PHRASE-Y do I need", how many Y does _user need.
######################################

# 25 first person utterances
On "I        need        PHRASE-X", _user needs X.
On "help", reply "to add items to the list".
On "get                  PHRASE-X", _user needs X.
On "get         Y        PHRASE-X", Y needs X.

On "I        have to have PHRASE-X", _user needs X.

On "I        have to     PHRASE-X", _user needs to X.
On "I do not have to     PHRASE-X", _user does not need to X.
On "I        need to     PHRASE-X", _user needs to X.
On "I        have to get PHRASE-X", _user needs X.
On "I        need to get PHRASE-X", _user needs X.
On "I do not have to get PHRASE-X", _user does not need X.
On "I do not need to get PHRASE-X", _user does not need X.

On "I do not need        PHRASE-X", _user does not need X.
On "I        have got    PHRASE-X", _user does not need X.
On "I        have        PHRASE-X", _user does not need X.
On "         clear the       list", _user has everything.
On "I        have got  everything", _user has everything.
On "I        have      everything", _user has everything.
On "I do not need        anything", _user has everything.
On "help", reply "to clear the list".

On                   "?", what does _user need.
On "what  do   i   need", what does _user need.
On "help", reply "to find out what is on the list".

On "do I  need PHRASE-X", does _user need X.
On "help", reply "to find out if something specific is on the list".

On "We need PHRASE-X", I need X. # fix for now

Enable undo. # do this at the end to expediate startup!